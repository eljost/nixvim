{
  plugins = {
    # Show buffers as tabs like in a browser ...
    bufferline.enable = true;
    # ... and use different icons according to the file extension
    web-devicons.enable = true;
  };
}
